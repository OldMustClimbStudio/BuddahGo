/// <summary>
/// Global network/gameplay debug switches.
/// Keep verbose logging disabled by default to avoid log flooding in multiplayer sessions.
/// </summary>
public static class NetDebug
{
    public static bool EnableVerboseLog = false;
}
